// TODO: Créer un script de chargement qui permet de faire patienter l'utilisateur avec une animation material design
function logError(message)
{
	$('body').prepend(message);
}
 $(".button-collapse").sideNav();
